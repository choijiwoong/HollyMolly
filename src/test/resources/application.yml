spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 'trianglestick0628'
    password: 'kczluwfjnoapgnof'
    properties:
      mail:
        smtp:
          starttls.enable: true
          auth: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace